services:
  - type: web
    name: industrial-ai-platform
    runtime: python
    plan: free
    buildCommand: "python --version"
    startCommand: "python deploy_app.py"
    healthCheckPath: /health.json
